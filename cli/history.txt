=== TOOLNET CLI — Development History ===

2026-07-30 — Phase 1: Slash commands foundation
- Repo root: /root/toolnetapi
  (Source backend at /root/ToolnetAPI, new CLI at /root/toolnetapi/packages/toolnet-cli)
- Gateway running on http://127.0.0.1:20128 (default, configurable)
- Auth: x-9r-cli-token header (reads ~/.toolnetapi/machine-id + ~/.toolnetapi/auth/cli-secret)

Bug fixed: SolidJS context (createContext/useContext) không tương thích với custom
reconciler của @opentui/solid. Giải pháp: module-level singleton (getGateway()) thay vì
React-style context. solid-js/dist/solid.js phải import đúng path để tránh server build.

New files:
  src/lib/gateway.ts       — API client + global singleton getGateway()
  src/commands/index.ts    — Command registry + dispatcher
  src/commands/help.ts     — /help command
  src/commands/status.ts   — /status command (health, providers, combos, keys, settings)
  src/commands/model.ts    — /model command (list models from /v1/models)
  src/commands/exit.ts     — /exit command
  src/screens/chat.tsx     — Chat screen with slash command parsing
  src/context/gateway.tsx  — Gateway context (deprecated, kept for reference)
  src/solid-js-dist.d.ts   — TypeScript declaration for solid-js/dist/solid.js

Modified files:
  src/app.tsx              — createGateway + ChatScreen
  src/index.tsx            — Keep alive

Phase 2 complete:
  src/commands/providers.ts  — /providers (list, add, test, delete, oauth, poll)
  src/commands/combos.ts     — /combos (list, create, delete)
  src/commands/settings.ts   — /settings (view, rtk, headroom, headroom-url)
  src/commands/keys.ts       — /keys (list, create, delete)
  src/commands/tools.ts      — /tools (CLI tools status)
  Registered in src/commands/index.ts

Phase 3 complete — coding agent tools:
  src/lib/codingAgent.ts     — Core tool functions (read, glob, grep, edit, write, bash)
  src/commands/read.ts       — /read <file> [offset] [limit]
  src/commands/glob.ts       — /glob <pattern> [path]
  src/commands/grep.ts       — /grep <pattern> [path] [--include=*.ts]
  src/commands/edit.ts       — /edit <file> <old> <new>, --replace-all, --read
  src/commands/write.ts      — /write <file> <content>
  src/commands/bash.ts       — /bash <command>
  src/commands/websearch.ts  — /websearch <query> (via /v1/search)
  src/commands/webfetch.ts   — /webfetch <url> (via /v1/web/fetch)
  src/commands/question.ts   — /question <text>
  All registered in src/commands/index.ts

Phase 4 — Undo/Redo, Session tabs, Config file:

  src/lib/history.ts     — Change snapshot stack with undo()/redo()/pushSnapshot()/commitSnapshot()
  src/lib/session.ts     — Multi-session manager (create, switch, rename, close, persist names to config)
  src/lib/config.ts      — CLI config file loader/saver (~/.toolnetapi/config.json)
  src/commands/undo.ts   — /undo command
  src/commands/redo.ts   — /redo command
  src/commands/config.ts — /config command (view/set baseUrl, defaultModel, theme, rtkEnabled)
  src/commands/session.ts — /session command (list, new, switch, delete, rename)
  codingAgent.ts         — pushSnapshot/commitSnapshot hooked into toolEdit, toolReplaceAll, toolWrite
  chat.tsx               — Session tab bar, Undo/Redo status indicators, keyboard shortcuts (Ctrl+Z/Y/T/W)

Keyboard shortcuts:
  Tab        — cycle sessions
  Ctrl+Z/Y   — undo/redo
  Ctrl+T     — new session
  Ctrl+W     — close current session
  Ctrl+↑/↓   — prev/next session (PgUp/PgDown)

All 4 phases complete.
