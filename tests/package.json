{
  "name": "toolnetapi-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Unit tests for ToolNet API",
  "scripts": {
    "test": "vitest run --config ./vitest.config.js --reporter=verbose",
    "test:watch": "vitest --config ./vitest.config.js --reporter=verbose"
  },
  "devDependencies": {
    "vitest": "^4.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
